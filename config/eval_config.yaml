# Evaluation Configuration
# Settings for running evaluation and generating audio samples

paths:
  audio_output_dir: "./eval_audio"
  checkpoints_dir: "/scratch2/kani_tts/au_500k/checkpoints/"

huggingface:
  upload_dataset: false                 # Set true to push evaluation results
  repo_name: "aleks-wordcab/kani-tts-evaluation-results"
  private: true

processing:
  num_proc: 32                           # Number of processes for data loading
